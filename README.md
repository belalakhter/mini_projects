## Data Structures
- DSAs Binary tree
- Redis implementation with resp format messages, tcp server and LFU evictor

## Realtime Poll
- User can vote only one time to one candidate and result are updated globally.
- Admin can set password to add candidates in backend.

### Redpanda kafka setup
Automate cluster setup for production grade kafka with system tune, TLS and kubernetes

### Worker Pool
Concurrency using worker threads
